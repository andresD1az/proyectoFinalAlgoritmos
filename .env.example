# =============================================================
# .env.example — Plantilla de variables de entorno
# INSTRUCCIONES: Copia este archivo como .env y cambia los valores
#   cp .env.example .env
# NUNCA subas el archivo .env real al repositorio
# =============================================================

# --- Render provee esta variable automáticamente al conectar una BD ---
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# --- Variables individuales (para desarrollo local con Docker) ---
DB_NAME=bvc_analytics
DB_USER=bvc_user
DB_PASSWORD=pon_aqui_tu_password_seguro
DB_HOST=localhost
DB_PORT=5432

# --- API ---
API_HOST=0.0.0.0
API_PORT=8001
